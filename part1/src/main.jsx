import ReactDOM from 'react-dom/client'

import App from './App'

//HTML element 'root' is created and the 'App.jsx' component is injected
ReactDOM.createRoot(document.getElementById('root')).render(<App />)
